{
  "mcpServers": {
    "redmine-agent": {
      "command": "npx",
      "args": [
        "-y",
        "@muzzamil-khan/redmine-agent-mcp"
      ],
      "env": {
        "REDMINE_BASE_URL": "${REDMINE_BASE_URL}",
        "REDMINE_API_KEY": "${REDMINE_API_KEY}",
        "REDMINE_ACTIVITY_ID": "${REDMINE_ACTIVITY_ID}",
        "REDMINE_BILLABLE_HOURS_FIELD_ID": "${REDMINE_BILLABLE_HOURS_FIELD_ID}"
      }
    },
    "redmine": {
      "url": "${REDMINE_SSE_URL}",
      "headers": {
        "x-api-key": "${REDMINE_SSE_API_KEY}",
        "x-redmine-api-key": "${REDMINE_API_KEY}"
      }
    },
    "google-sheets": {
      "command": "uvx",
      "args": [
        "--with",
        "mcp<2",
        "mcp-google-sheets@latest"
      ],
      "env": {
        "CREDENTIALS_PATH": "${GOOGLE_SHEETS_CREDENTIALS_PATH}",
        "TOKEN_PATH": "${GOOGLE_SHEETS_TOKEN_PATH}"
      }
    }
  }
}
